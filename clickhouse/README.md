Sqitch ClickHouse Docker Image
==============================

Build an image named `sqitch-clickhouse` with this command from the root
directory:

``` sh
env env DIR=clickhouse ARCHS=amd64 ./build
```
